#!/usr/bin/env krb -f

if (@argc() != 4) {
  print "Usage: krb -f examples/cat.krb <file>";
  @exit(1);
}

let path: string = @argv(3);

if (!@fileExists(path)) {
  print $"File doesn't exist '{path}'";
  @exit(1);
}

let fileText = @readFileToString(path);

@println(fileText);
