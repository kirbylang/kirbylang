if (argc() != 5) {
  print "Usage: clox -f examples/cat.lox <file>";
  exit(1);
}

var fileText = {
  var path = argv(4);

  if (!fileExists(path)) {
    print "File doesn't exist '" + path + "'";
    exit(1);
  }

  readFileToString(path)
};

print fileText;
