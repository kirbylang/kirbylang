#!/usr/bin/env krb -f

fun fib(n: f64): f64 {
  if (n < 2) return n;

  fib(n - 2) + fib(n - 1)
}

print "Fibonacci";

var arg = @argv(4) ?? @prompt("n: ");
var n = @parseNumber(arg);

print $"{n}\n";

var start = @clock();

print $"Result:\n{fib(n)}\n";

print $"Time Taken: {@clock() - start}";
