#!/usr/bin/env krb -f

struct Zoo {
  var aardvark;
  var baboon;
  var cat;
  var donkey;
  var elephant;
  var fox;
}

impl Zoo {
  pub fun init() = Zoo {
    aardvark: 1,
    baboon: 1,
    cat: 1,
    donkey: 1,
    elephant: 1,
    fox: 1,
  };

  pub fun ant(self) = self.aardvark;
  pub fun banana(self) = self.baboon;
  pub fun tuna(self) = self.cat;
  pub fun hay(self) = self.donkey;
  pub fun grass(self) = self.elephant;
  pub fun mouse(self) = self.fox;
}

var zoo = Zoo.init();
var start = clock();

var sum = 0;

while (sum < 100000000) {
  sum = sum + zoo.ant()
            + zoo.banana()
            + zoo.tuna()
            + zoo.hay()
            + zoo.grass()
            + zoo.mouse();
}

print clock() - start;
print sum;
