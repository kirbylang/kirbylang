var arg = argv(4) ?? prompt("limit: ");
var limit = parseNumber(arg);

fun fizzbuzz(n) = if (n % 5 == 0 and n % 3 == 0) {
    "FizzBuzz"
} else if (n % 3 == 0) {
    "Fizz"
} else if (n % 5 == 0) {
    "Buzz"
} else {
    n
};

for (var i = 1; i <= limit; i = i + 1) {
    print fizzbuzz(i);
}
