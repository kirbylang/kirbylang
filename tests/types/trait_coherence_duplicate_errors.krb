trait Display {
    fun toString(self): string;
}

struct Point {
    pub var x: f64;
}

impl Display for Point {
    pub fun toString(self): string = "first";
}

impl Display for Point {
    pub fun toString(self): string = "second";
}
