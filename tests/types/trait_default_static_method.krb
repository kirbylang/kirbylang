trait Default {
    fun default(): Self;
}

struct Point {
    pub var x: f64;
    pub var y: f64;
}

impl Default for Point {
    pub fun default(): Self = Point { x: 0, y: 0 };
}

var origin: Point = Point.default();
print origin.x;
print origin.y;
