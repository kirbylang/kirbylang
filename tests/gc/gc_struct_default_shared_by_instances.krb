// The same default is copied into every instance. If it were collected after
// the first instantiation, later instances would read freed memory.
struct S {
    pub var items = [10, 20];
}

var a = S {};
var b = S {};
var c = S {};
print a.items;
print b.items;
print c.items;
