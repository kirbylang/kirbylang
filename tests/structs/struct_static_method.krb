struct Point {
    pub var x;
    pub var y;
}

impl Point {
    fun new(x, y) = Point { x: x, y: y };

    pub fun origin() = Point.new(0, 0);
}

var point = Point.origin();

print point.x;
print point.y;
