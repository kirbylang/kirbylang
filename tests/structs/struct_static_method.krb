struct Point {
    pub var x: f64;
    pub var y: f64;
}

impl Point {
    fun new(x: f64, y: f64): Point = Point { x: x, y: y };

    pub fun origin(): Point = Point.new(0, 0);
}

var point = Point.origin();

print point.x;
print point.y;
