struct Counter {
    var count;
}

impl Counter {
    pub fun new() = Counter { count: 7 };

    // A lambda written inside a method inherits that method's access.
    pub fun read(self) {
        var read = fun () { self.count };

        read()
    }
}

print Counter.new().read();
