struct Counter {
    var count = 7;
}

impl Counter {
    // A lambda written inside a method inherits that method's access.
    pub fun read(self) {
        var read = fun () { self.count };

        read()
    }
}

print Counter {}.read();
