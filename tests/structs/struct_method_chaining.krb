struct Chain {
    var value;
}

impl Chain {
    pub fun new(value) = Chain { value: value };

    pub fun plus(self, n) = Chain.new(self.value + n);

    pub fun get(self) = self.value;
}

print Chain.new(1).plus(2).plus(3).get();
