struct Counter {
    pub var count;
}

impl Counter {
    // Access travels with the closure, so it survives leaving the method.
    pub fun reader(self) {
        var reader = fun () { self.count };

        reader
    }
}

var read = Counter { count: 7 }.reader();

print read();
