struct Account {
    var balance: f64;
}

impl Account {
    pub fun new(): Account = Account { balance: 100 };

    pub fun deposit(self, amount: f64): Account {
        self.balance = self.balance + amount;
        self
    }

    pub fun balance(self): f64 = self.balance;
}

print Account.new().deposit(50).balance();
