struct Account {
    var balance;
}

impl Account {
    pub fun new() = Account { balance: 100 };

    pub fun deposit(self, amount) {
        self.balance = self.balance + amount;
        self
    }

    pub fun balance(self) = self.balance;
}

print Account.new().deposit(50).balance();
