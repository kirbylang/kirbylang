struct Greeter {
    var name;
}

impl Greeter {
    pub fun new() = Greeter { name: "World" };

    fun subject(self) = self.name;

    pub fun greet(self) = "Hello " + self.subject();
}

print Greeter.new().greet();
