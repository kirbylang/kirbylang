struct Greeter {
    var name: string;
}

impl Greeter {
    pub fun new(): Greeter = Greeter { name: "World" };

    fun subject(self): string = self.name;

    pub fun greet(self): string = "Hello " + self.subject();
}

print Greeter.new().greet();
