struct Account {
    var balance: f64;
}

impl Account {
    pub fun withBalance(amount: f64): Account = Account { balance: amount };

    pub fun balance(self): f64 = self.balance;
}

print Account.withBalance(250).balance();
