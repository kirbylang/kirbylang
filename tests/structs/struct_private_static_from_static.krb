struct Point {
    var _x: f64;
}

impl Point {
    fun make(x: f64): Point = Point { _x: x };

    pub fun origin(): Point = Point.make(0);

    pub fun x(self): f64 = self._x;
}

print Point.origin().x();
