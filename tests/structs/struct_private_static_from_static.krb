struct Point {
    var x: f64;
}

impl Point {
    fun make(x: f64): Point = Point { x: x };

    pub fun origin(): Point = Point.make(0);

    pub fun x(self): f64 = self.x;
}

print Point.origin().x();
