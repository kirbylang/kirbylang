// The `pub` on `label` must not leak to the field declared after it.
struct Config {
    pub var label;
    var token;
}

impl Config {
    pub fun new(label, token) = Config {
        label: label,
        token: token,
    };
}

var c = Config.new("visible", "hidden");

print c.label;
print c.token;
