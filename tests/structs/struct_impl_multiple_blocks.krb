struct Counter {
    pub var count;
}

impl Counter {
    pub fun increment(self) {
        self.count = self.count + 1;
    }
}

impl Counter {
    pub fun get(self) {
        return self.count;
    }
}

var counter = Counter { count: 0 };

counter.increment();
counter.increment();

print counter.get();
