struct Counter {
    var count = 0;
}

impl Counter {
    pub fun increment(self) {
        self.count = self.count + 1;
    }
}

impl Counter {
    pub fun get(self) {
        return self.count;
    }
}

var counter = Counter();

counter.increment();
counter.increment();

print counter.get();
